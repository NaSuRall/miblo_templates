use serde::{Serialize, Deserialize};

#[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)]
pub struct {{ model_name_up }} {
    {{#each fields}}
        pub {{ name }}: {{ type }},
    {{/each}}
}

#[derive(Debug, Clone, Serialize, Deserialize, sqlx::FromRow)]
pub struct {{ model_name_up }}Option {
    {{#each fields}}
        pub {{ name }}: Option<{{ type }}>,
    {{/each}}
}
