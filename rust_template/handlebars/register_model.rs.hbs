use serde::{Deserialize};


#[derive(Deserialize)]
pub struct RegisterUser {
    pub last_name: String,
    pub first_name: String,
    pub pseudo: String,
    pub email: String,
    pub password: String,
    pub phone: String,
}
